# MySQL connection
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lender_service?useSSL=false
    username: root
    password: Bongo@501
  jpa:
    spring:
      jpa:
        database-platform=org:
          hibernate:
            dialect:
              MySQL8Dialect:

    hibernate:
      ddl-auto: update
    show-sql: true
